like:模糊查询
%:相当于*通配
    ------>通配的时候一定要带'' 
    ------>还要区分大小写
    ------>这个也就相当于一种Patter

_:匹配一个字符
    ------>同理

    ========>转义%和_
            关键字 escape :escape 后面的字符,使我们自定义的转义字符
            使用: select *from emp  where ename like 'x\_x' escape '\'


select 指定列
from: 指定表
where:指定条件

客户端,将指令给实例
实例来解析指令
将解析的指令给服务器

